venv/
chroma_db/
.env
__pycache__/
*.pyc
.DS_Store

# 数据库文件（含WAL和SHM）
*.db
*.db-shm
*.db-wal

# 用户上传
uploads/

# 备份
backups/

# 旧版备份代码
*_old.py

# 本地 embedding 模型（bge 权重 390MB，不入库；首次拉取按 README/文档说明下载）
models/

# 原始抓取内容（网文/微信读书导出，受版权保护，个人存档可、传播不可）
inputs/

# 临时工具目录/压缩包（抓取工具含登录态，绝不入 git）
tmp_*/
tmp_*.zip

# 临时备份
*.bak-*
